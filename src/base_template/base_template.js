const temp_model = require('./model/model')
const temp_page = require('./pages/page')
const temp_vm = require('./vm/vm')
const temp_vo =  require('./entity/example_entity')

const config = {
    temp_model: temp_model,
    temp_page: temp_page,
    temp_vm:temp_vm,
    temp_vo:temp_vo
}

module.exports = config
