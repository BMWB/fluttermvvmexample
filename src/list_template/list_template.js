const temp_model = require('./model/example_model')
const temp_page = require('./pages/example_page')
const temp_vm = require('./viewmodel/example_viewModel')
const temp_vo = require('./entity/example_entity')

const config = {
    temp_model: temp_model,
    temp_page: temp_page,
    temp_vm:temp_vm,
    temp_vo:temp_vo
}

module.exports = config
